{{- if not .Values.gateway.serviceAccount.name }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "argus.fullname" . }}-gateway
  namespace: {{ .Release.Namespace }}
  labels: {{ include "argus.labels" . | nindent 4 }}
{{- end }}
